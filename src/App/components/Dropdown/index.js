// import React, { Fragment } from "react";
// import PropTypes from "prop-types";

// const Dropdown = ({ items }) => {
//     return (
//         <ul className="dropdown" data-dropdown="true">{"\n"}
//             {items.map(({ label, href, icon }, i) => {
//                 const attr = {
//                     href: href || "#",
//                     "data-dropdown-icon": icon || null
//                 };

//                 return (
//                     <Fragment key={`dd-item-${i}-${label}`}>
//                         <a {...attr}>{label}</a>{"\n"}
//                     </Fragment>
//                 );
//             })}
//         </ul>
//     );
// };

// Dropdown.propTypes = { items: PropTypes.array.isRequired };

// export default Dropdown;
