import React from "react";
import PropTypes from "prop-types";

const Card = props => {
    return (
        <section className="card">
            asd
        </section>
    );
};

Card.propTypes = {};

export default Card;
